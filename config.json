{
    "//": "相对文件所在文件夹的路径",
    "model_path":"",
    "cache_path": "cache/",
    "log_path": "log/",
    "//": "log记录的方式，0:只保存到文件，1:只显示到终端，2:既显示到终端又保存到文件",
    "log2type": 2,
    "//use_gpu":true,
    "debug":false,
    "host": "0.0.0.0",
    "port": 50086,
    "device":"cuda",
    "//":"device为cuda时，更改使用的gpu编号，device为cpu不影响",
    "gpus":[2],
    "//":"使用自己训练的网络得到初步的身体分割图和头发方向图，否则使用faceparsing分割得到身体和头发分割图",
    "use_step":true,
    "//":"使用segment anything作为精确分割算法,必选true",
    "use_strand":true,
    "//":"使用第三个modeling网络,不管use_hd,voxel_size 都是 192,256,256",
    "use_modeling":true,
    "//":"use_ori_addinfo (bool, optional): 使用方向图作为ModelingHD网络的输入，否则使用归一化后的头发深度图作为输入. Defaults to False.",
    "use_ori_addinfo":false,
    "//":"使用voxel_size = 192,256,256，use_modeling时固定为192,256,256",
    "use_hd":false
}
